using UnityEngine;
//using InControl;

namespace Kunstharz {
	public class InControlTestScript : MonoBehaviour {

        void Start() {

        }

        void Update() {
            /*
            // get active device
            InputDevice device = InputManager.ActiveDevice; 
            
            // map certain part of device
            InputControl control = device.GetControl(InputControlType.LeftStickUp);

            // if left stick moved
            if(control.HasChanged){
                Debug.Log("controller moved");
            }

            // Checking devices
            InputManager.OnDeviceAttached += inputDevice => Debug.Log( "Attached: " + inputDevice.Name );
            InputManager.OnDeviceDetached += inputDevice => Debug.Log( "Detached: " + inputDevice.Name );
            InputManager.OnActiveDeviceChanged += inputDevice => Debug.Log( "Switched: " + inputDevice.Name );
            */
        }

    }
    
}